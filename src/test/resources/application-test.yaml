app:
    external-payment:
        fake-enabled: true

    payment:
        retry:
            scheduler:
                enabled: false

        outbox:
            publisher:
                enabled: false